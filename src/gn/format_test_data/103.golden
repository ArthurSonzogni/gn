# NOTE: This target is separated from :browser as
# //components/safe_foo_bar_/content/browser/triggers, which this depends on,
# depends on :browser.
# TODO(crbug.com/12345678): Consider folding all of three these together into
# :browser, along with the other browser process code in
# //components/safe_foo_bar_/content.
